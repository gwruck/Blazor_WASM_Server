@****Ths is a standard _Host file from a Blazor Server project*@
@****Note the svrWASM prefix*@

@page "/svr"
@using Blazor_WASM_Server.Server
@using Microsoft.AspNetCore.Mvc.TagHelpers

@*@attribute [Authorize]*@

@namespace Blazor_WASM_Server.Host.Pages
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    Layout = "_Layout";
}

@****Note that it uses a different App as the Client side components do*@
<component type="typeof(AppHost)" render-mode="ServerPrerendered" />
